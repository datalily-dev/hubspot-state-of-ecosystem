/**
 * ScrollManager — placeholder for per-page scroll behavior orchestration.
 * Will be fleshed out when individual pages are built.
 * Each page registers its scroll type: 'advance' or 'inner'.
 */
export default function ScrollManager({ children }) {
  return <div>{children}</div>;
}
