# Dynamic page content files go here.
# One JSON file per filter combination (17 total).
# File naming: {filterId}.json, e.g. solutions-smb-nam.json
# See src/data/filters.json for all valid filter IDs.
# See the project README for the content data model.
